pipeline {
  agent {label 'agent1'}

  environtment {

  }

  stages {
    stage('Hello') {
      steps {
        echo "${WORKSPACE}"
        sh '''
          ansible --version
          ansible-playbook --version
          ansible-galaxy --version
        '''
      }
    }
  }
}